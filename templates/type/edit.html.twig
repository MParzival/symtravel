{% extends 'base.html.twig' %}

{% block title 'Modifier un type'%}
{% block body %}
    <h1>Modifier un type</h1>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary pull-right">save</button>
    {{ form_end(form) }}

{% endblock %}


